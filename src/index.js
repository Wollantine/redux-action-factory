import {
    createAction,
    createRawAction,
    type,
    initialize,
    getConfig
} from './action-factory-lib';

export default {
    createAction,
    createRawAction,
    type,
    initialize,
    getConfig
};
